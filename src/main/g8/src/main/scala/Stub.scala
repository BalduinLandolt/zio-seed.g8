class Stub {
  // got any helpful boilerplate for your users?
  /*
  name = $name$
  description = $description$
  scala3 = $scala3$ -> $if(scala3.truthy)$ yes $else$ no $endif$
  scalatest_version = $scalatest_version$
  */
}
